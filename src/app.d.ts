// Fully client-side app — no server Locals. Kept for SvelteKit's App namespace.
declare global {
  namespace App {
    // interface Error {}
    // interface Locals {}
    // interface PageData {}
    // interface Platform {}
  }
}

export {};
